Same as a post markdown

Requirements:
File names are sorted and displayed in that order. So, prefix date like this for every file. 2015-02-28
File names should not contain any spaces. If you need a separator, use hyphen.
They should have a header-thumb-img in the yaml front matter along with header-img